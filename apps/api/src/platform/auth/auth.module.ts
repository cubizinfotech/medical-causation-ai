import { Module } from '@nestjs/common';

/**
 * Shared authentication module scaffold.
 * Wire JWT/Passport providers here in a later phase — do not put MCA/EWI logic here.
 */
@Module({})
export class AuthModule {}
