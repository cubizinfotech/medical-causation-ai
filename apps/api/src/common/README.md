# Cross-cutting Nest utilities (guards, filters, pipes)

Product-agnostic Nest utilities belong here when implemented.

Shared platform contracts live in `src/platform/`.
Shared document/RAG Nest modules are composed via `src/modules/common/`.
